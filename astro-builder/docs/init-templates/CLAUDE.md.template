# CLAUDE.md

This file provides guidance to Claude Code when working in this repository.
Generated by `astro-builder` plugin. Update by re-running `/astro-builder:init`.

## Project Overview

{{PROJECT_DESCRIPTION}}

**Site name**: {{SITE_NAME}}
**Target audience**: {{TARGET_AUDIENCE}}
**Deployed at**: {{SITE_URL}}
**Deployment target**: {{DEPLOYMENT_TARGET}}

## Development Commands

```bash
pnpm install          # Install dependencies (pnpm only — never npm or yarn)
pnpm dev              # Start dev server
pnpm build            # Production build
pnpm preview          # Preview production build
```

**Quality gates** (every commit must pass):

```bash
pnpm build            # Zero build errors
tsc --noEmit          # Zero TypeScript errors
biome check .         # Zero lint/format errors
biome check --write . # Auto-fix formatting before committing
```

## Languages

- **Default locale**: {{DEFAULT_LOCALE}}
- **All locales**: {{ALL_LOCALES}}
- **URL pattern**: All locales prefixed (e.g. `/{{DEFAULT_LOCALE}}/`, `/{{SECONDARY_LOCALE}}/`)

## Architecture

### Page-views pattern (mandatory)

Three layers — never collapse them:

1. **Pages** (`src/pages/{{DEFAULT_LOCALE}}/`) — Thin wrappers (≤5 lines). Import a PageView, nothing else.
2. **Page-Views** (`src/page-views/`) — All markup, data fetching, and translations.
3. **Layouts** (`src/layouts/`) — Page shell (html, head, body, header, footer).

### i18n

- All UI strings in `src/i18n/{{DEFAULT_LOCALE}}.json` + `src/i18n/{{SECONDARY_LOCALE}}.json`
- Use `createTranslator(Astro.currentLocale)` in every component — never pass `lang` or `tl` as props
- Detect locale via `Astro.currentLocale` — never parse URLs
- `prefixDefaultLocale: true` — all routes prefixed
- `redirectToDefaultLocale: false` + explicit `redirects: { '/': '/{{DEFAULT_LOCALE}}' }`

### Content collections

- Config at `src/content.config.ts` (NOT `src/content/config.ts`)
- `glob()` loaders, Zod schemas
- Single collection per type with locale subfolders: `articles/{{DEFAULT_LOCALE}}/`, `articles/{{SECONDARY_LOCALE}}/`
- `translationKey` links content across locales
- Flexible `tags: string[]` — no fixed category enums

### Utility layer (`src/lib/`)

- `content.ts` — all content collection queries
- `urls.ts` — all URL builders
- `format.ts` — locale-aware date/number formatting
- `i18n.ts` — `createTranslator()` factory

### Path aliases (always use these)

```
@/*            → src/*
@components/*  → src/components/*
@layouts/*     → src/layouts/*
@styles/*      → src/styles/*
@lib/*         → src/lib/*
@page-views/*  → src/page-views/*
@i18n/*        → src/i18n/*
```

### Tooling

- Package manager: `pnpm` (never npm or yarn)
- Linting/formatting: Biome (never ESLint or Prettier)
- Config: `astro.config.ts` (never `.mjs`)
- Node.js: 22 LTS

## Project-specific context

See `.astro-builder/` for full context:

- `.astro-builder/content-schema.md` — Content types, fields, relationships
- `.astro-builder/design-system.md` — Colors, typography, component patterns
- `.astro-builder/style-guide.md` — Voice, tone, writing conventions
- `.astro-builder/anti-patterns.md` — What to avoid in this specific project

## Workflow rules

- Every task ends with a git commit (descriptive message)
- Always follow Astro 6 docs: https://docs.astro.build/llms-small.txt
- Use only official Astro integrations (`@astrojs/sitemap`, `@astrojs/rss`, `@astrojs/mdx`)
- Follow MDN Web API references for browser API usage: https://developer.mozilla.org/en-US/
